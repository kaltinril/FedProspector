# Database
DB_HOST=localhost
DB_PORT=3306
DB_NAME=fed_contracts
DB_USER=fed_app
DB_PASSWORD=changeme

# MySQL binary directory (only needed if mysqld is not on PATH)
# MYSQL_BIN_DIR=D:\mysql\bin

# SAM.gov API (get key at sam.gov, expires every 90 days)
SAM_API_KEY=your_api_key_here
SAM_DAILY_LIMIT=10

# Directories
DATA_DIR=./data
LOG_LEVEL=INFO
